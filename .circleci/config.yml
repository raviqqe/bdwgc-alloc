version: 2
jobs:
  build:
    docker:
      - image: rust
    steps:
      - checkout
      - run: git submodule update --init --recursive
      - run: cargo build
      - run: cd examples && ./test.sh
